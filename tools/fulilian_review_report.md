- 2026-09-16 (later): file 12 `tools/bot_failure_reasons.py` (172 L, full read) reviewed — **C4-9** (低, quota rule matches bare substring `balance` inside "unbalanced" without word boundaries → wrong typed reason + missed auto-retry; conservative direction, no runaway risk). Retry-policy ↔ maintainer ruling consistency and classifier precedence verified sound. §2 ledger updated: **125 issues**. Branch 4 progress: 12/164 files. Next: `tools/bot_mode_dm.py` (alphabetical, 765 L).
- 2026-09-16 (later): file 13 `tools/bot_mode_dm.py` (765 L, full read) reviewed — **C4-10** (低, `_delivery_lock` derives the per-profile turn-lock root from ambient env instead of the agent's session-db home — C0-1 class; in override contexts two deliveries can race the same profile's Bot Chat turn). Containment contract (double title-gate, roster validation, temp-file transport, 0700 dir ownership checks, cleanup ownership transfer) verified sound. §2 ledger updated: **126 issues**. Branch 4 progress: 13/164 files. Next: `tools/bot_mode_probe.py` (alphabetical, 469 L).
- 2026-09-16 (later): file 14 `tools/bot_mode_probe.py` (469 L, full read) reviewed — **PASS, 0 issues**. Verified `set_fulilian_home_override` is a ContextVar (fulilian_constants.py:17) so the scoped config-load inside `capability_fingerprint` cannot leak across threads; epoch staleness/upgrade checks fail closed and are loop-proof. Ledger stays at 126. Branch 4 progress: 14/164 files. Next: `tools/bot_relay.py` (alphabetical, 676 L).
- 2026-09-16 (later): file 15 `tools/bot_relay.py` (677 L, full read) reviewed — **PASS, 0 issues**. Untrusted-roster validation, atomic writes, envelope-id traversal guard, flock turn-lock budget, waiter-injection defense all verified sound; `_remote_paragraph` zip pairing confirmed order-consistent (closes that probe observation). Ledger stays at 126. Branch 4 progress: 15/164 files. Next: `tools/browser_tool.py` family (alphabetical — `browser_*.py`, largest `browser_tool.py` 6,192 L).
- 2026-09-16 (later): file 16 `tools/browser_camofox.py` (974 L, full read) reviewed — **C4-11** (低, `_get_session` performs the tab-adoption HTTP GET while holding `_sessions_lock` — all camofox calls serialize behind a hung server). Secret handling / opt-in loopback rewrite / SSRF read-path blocks / typed-text + vision redaction verified sound; navigate-side SSRF pre-check flagged as a §7 cross-check for browser_tool.py. §2 ledger updated: **127 issues**. Branch 4 progress: 16/164 files. Next: `tools/browser_camofox_state.py` (alphabetical, 47 L).
- 2026-09-16 (later): file 17 `tools/browser_camofox_state.py` (47 L, full read) reviewed — **PASS, 0 issues**. Ledger stays at 127. Branch 4 progress: 17/164 files. Next: `tools/browser_cdp_tool.py` (alphabetical, 691 L).
